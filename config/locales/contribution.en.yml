en:
  activerecord:
    errors:
      models:
        contribution:
          attributes:
            amounts:
              invalid_currency: "Invalid currency"
    attributes:
      contribution:
        amount_cents: "Contribution amount"
        amount: "Contribution amount"
  errors:
    messages:
      amount_less_than_minimum: must be $1,00 at least
      amount_out_of_bounds: exceed the bounds