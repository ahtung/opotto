tr:
  activerecord:
    attributes:
      contribution:
        amount_cents: "Katkı miktarı"
  errors:
    messages:
      amount_less_than_minimum: en az $1,00 olmalı
      amount_out_of_bounds: katılım sınırlarını aşıyor