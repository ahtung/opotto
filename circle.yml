test:
  override:
    - bundle exec rake spec
    - bundle exec rake quality
    - bundle exec rake security
deployment:
  staging:
    branch: develop
    commands:
      - ./script/deploy/staging
  production:
    branch: production
    commands:
      - ./script/deploy/staging
