test:
  override:
    - bundle exec rake test
  post:
    - bundle exec rake quality
deployment:
  staging:
    branch: develop
    commands:
      - ./script/deploy/staging.sh
  production:
    branch: production
    commands:
      - ./script/deploy/staging.sh